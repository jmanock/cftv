import React from 'react';

const Bikes = () =>{
  const styles = {
    color:'darkRed',
    textAlign:'center',
    marginTop:100
  }
  return(
    <section className='bikes'>
      <h1 style={styles}>What We Do, &amp; Why</h1>
    </section>
  );
};

export default Bikes;
